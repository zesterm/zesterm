{
  "name": "@zesterm/proto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The zesterm wire protocol in TypeScript: framing, MessagePack, and the delta decoder.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
