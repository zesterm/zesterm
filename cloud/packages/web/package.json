{
  "name": "@zesterm/web-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Worker that serves the built web client and its /api surface.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --disable-warning=ExperimentalWarning \"test/**/*.test.ts\"",
    "dry-run": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.28.0"
  },
  "dependencies": {
    "@zesterm/cloud-shared": "workspace:*"
  }
}
