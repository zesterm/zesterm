/target
**/*.rs.bk
*.pdb

# Recorded VT corpora are large; the small curated set under
# crates/zest-core/tests/corpus is committed, ad-hoc captures are not.
/captures
*.vtrec.tmp

node_modules/
*.tsbuildinfo

# Generated
/bindings
/dist
NOTICE.generated

.DS_Store
Thumbs.db

# Offscreen render output from the dump examples
/*.png

# wrangler's cache, which holds the Cloudflare account id. `cloud/.gitignore`
# covers the one `cloud/` produces; this covers the one that appears wherever
# wrangler is invoked from, which includes the repo root.
.wrangler/
